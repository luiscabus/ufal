#include <stdio.h>

// 1 Defina uma matriz 3x4 de inteiros na Pilha de Execução (PE) - MAIN
// 2 Defina um array de 12 inteiros na PE - MAIN
// 3 Defina um array de 12 inteiros na HEAP - MAIN
// 4 Leia matriz - FUNÇÃO
// 5 Escreva no array os valores da matriz - FUNÇÃO toArray
// 6 Escreva uma função que recebe o array i e j e imprime o elemento a(i,j) da matriz. - FUNÇÃO printArray
// 7 Escreva uma função que recebe uma matriz i,j e imprime o elemento a(i,j) da matriz. - FUNÇÃO printMatrix 


void montar_matriz(int cols, int matriz[][cols]) {
	
}

int main() {
	int matriz[3][4]; //1
	int array[12]; //2
	
	int* frog = new int[75];
	int* array_heap = (int*) malloc(12*sizeof(int)); //3	
	
// 	int** matriz_heap = (int**) malloc(3*sizeof(int*)); //3	
// 	matriz_heap[0] = (int**) malloc(4*sizeof(int*)); //3	
	
	int* = malloc(12*sizeof(int));
	
	montar_matriz(4, matriz);
	
	
}